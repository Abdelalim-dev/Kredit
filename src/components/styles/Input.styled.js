import styled from "styled-components";
import { TextInput, HelperText } from 'react-native-paper';

export default InputStyled = styled(TextInput)`
`

export const HelperTextStyled = styled(HelperText)`
`